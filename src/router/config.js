import Login from './../pages/login/index'
import Main from './../pages/main/index'

const routerConfig = [
    {
        path: '/login',
        component: Login,
    },
    {
        path: '/main',
        component: Main,
    }
];

export default routerConfig